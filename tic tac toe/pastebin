Απλα πεταω τυχαιες μαλακιες εδω.




    # Προσθετει +1 στην γυρα και μετα το περναει απο το check_turn για να δει πιος θα παιξει
    # Στο "grid[int(choice)]" Περνει το input (το μετατρεπει σε int ωστε να τεριαζει με το grid)
    # και αντικαθηστα το συγκεκριμενο σημειο στον πινακα με x ή o αναλογα με το check_turn
    turn += 1
    grid[int(choice)] = check_turn(turn)